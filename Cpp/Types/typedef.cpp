#include <iostream>

using namespace std;

int main(){
    typedef int Boolean; // creates an alias, Boolean, for the data type int

    const Boolean TRUE = 1;
    const Boolean FALSE = 0;
    Boolean flag;
}